{
  "name": "bot_manager",
  "version": "0.0.0",
  "description": "A service for monitoring when we need to reload our running containers.",
  "main": "app.js",
  "author": "Johnny Hausman",
  "license": "MIT",
  "dependencies": {
    "ab-utils": "Hiro-Nakamura/ab-utils",
    "express": "^4.16.4",
    "nodemon": "^1.18.10",
    "shelljs": "^0.8.3",
    "slackbots": "^1.2.0"
  },
  "scripts": {
    "dev": "node_modules/.bin/nodemon --inspect=0.0.0.0:9229 app.js",
    "devBuild": "rm node_modules/.yarn-integrity && docker run -it --mount type=bind,source=\"$(pwd)\",target=/app -w /app node yarn",
    "devInstall": "docker run -it --mount type=bind,source=\"$(pwd)\",target=/app -w /app node bash"
  }
}
